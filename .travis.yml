# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R
sudo: true
latex: false
dist: trusty
cache: packages

r_binary_packages:
  - units
  - sf
  - httr

addons:
  apt:
    packages:
      - libudunits2-dev
